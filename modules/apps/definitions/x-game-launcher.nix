{
  # No type, defaults to "externally-managed"

  id = "steam"; # The command to run
  key = "x";
  # For externally-managed, mkApp will set installMethod = "none".
  # appId for Sway will default to "steam" (derived by helper from id).
}
